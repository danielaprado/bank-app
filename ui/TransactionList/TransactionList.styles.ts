import { Text } from "react-native";
import styled from "styled-components";

export const Container = styled(Text)`
  font-size: 16px;
  font-weight: bold;
  margin-left: 24px;
  margin-bottom: 16px;
`;
